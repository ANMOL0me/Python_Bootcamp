to initiate virtual enviornment

#python -m venv my_env
my_env is enviornment name.

to activate we use
#./my_env\Scripts\activate

deactivate to exit 

pip list command to see all installed .
 or we can create a file which has all packages.
 pip install -r requirments.txt
 to create requirments file = pip freeze > requirments.txt