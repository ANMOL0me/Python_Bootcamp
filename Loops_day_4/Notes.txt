# Conditionals 
1> 'If (condition) :'
2> for 2 condition we use 'Elif (condition) :'
3> for default we use 'else :'

=> Match Conditionals
=> For loops
|> for i in range():
|> for i in var:
=>While loop
|> while condition:
|> while true used for infinite loop


===> Loop control statenment
=> BREAK statenment break the loop.
=> CONTINUE statenment continue the loop.
=> PASS statenment when nothing to do.

To print a string several time i can use '  "*"*i n'
To reverse a num we can use (num)[::-1]