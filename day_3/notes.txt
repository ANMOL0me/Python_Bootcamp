INPUT METHOD
=>To take input from user we use a keyword "input"
  var_name = input
=> \n used for line change.

ADD METHOD
=>using print(a + b) doesn't add two no. it only prints it .
print(3 + 5) = 35 .
=>in this case print(a + 5)
we will think it will work but in starting if we declare a = input() it will automatically take it as string.
To make it woerking we use typecasting.
a = int(input())
     OR
a = input()
a = int(a)     